@model IEnumerable<ProductSearch.Models.SalesOrder>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Item.ItemID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderQnty)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SalesDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Item.ItemID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderQnty)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.SalesDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SalesOrderID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SalesOrderID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SalesOrderID })
        </td>
    </tr>
}

</table>
